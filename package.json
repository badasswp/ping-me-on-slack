{
	"name": "ping-me-on-slack",
	"version": "1.2.1",
	"description": "Get notifications on Slack when changes are made on your WP website.",
	"author": "badasswp",
	"license": "GPL-3.0-or-later",
	"scripts": {
		"start": "yarn wp-env start",
		"stop": "yarn wp-env stop",
		"boot": "composer install && yarn install && yarn start",
		"wp-env": "wp-env",
		"setup": "chmod +x bin/setup.sh && bin/setup.sh",
		"bash": "wp-env run cli bash"
	},
	"devDependencies": {
		"@wordpress/env": "^10.20.0",
		"@wordpress/eslint-plugin": "^22.6.0",
		"@wordpress/scripts": "^26.9.0"
	}
}
